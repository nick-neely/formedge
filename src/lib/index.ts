// Entry point for npm package
// Add exports for components, hooks, utils, etc
// export { default as FormBuilder } from './components/FormBuilder';

export default function init() {
  console.log("FormEdge package initialized.");
}
